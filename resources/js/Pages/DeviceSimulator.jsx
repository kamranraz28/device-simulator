import React from 'react';
import DeviceSimulator from '../Components/DeviceSimulator';

export default function DeviceSimulatorPage({ initialPresets }) {
  return <DeviceSimulator initialPresets={initialPresets} />;
}
